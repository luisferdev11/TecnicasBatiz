Si quieres ejecutar los programas usa tu "virtual envioronment" de Python, en este caso ningun programa lo lleva porque creé un venv por programa y no usé el global, por lo que pesa mucho xd.
La parte del proyecto aula de python se encuentra en el siguiente link: https://github.com/luisferdev11/BearCareful (incluye cosas extra como modulos de flask y una regresión lineal que considero más simple).

If you want to run the programs use your Python "virtual envioronment", in this case no program has it because I created one venv per program and I didn't use the global one, so the file is very heavy xd.
Final python project including flask modules and linear regression here: https://github.com/luisferdev11/BearCareful.